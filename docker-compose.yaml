version: "3.9"

services:
  transcoding-service:
    build:
      context: .
      dockerfile: docker/Backend.Dockerfile
    image: transcoding-service
    container_name: transcoding-service
    user: "${UID}:${GID}" 
    environment:
      - MEDIA_DIR=/app/media
      - SERVER_CONFIG_PATH=/app/configs/media_server/4s_segments_8i.yaml
    volumes:
      - ./media:/app/media
      - ./configs:/app/configs
    networks:
      - servernet

  media-server:
    build:
      context: .
      dockerfile: docker/MediaServer.Dockerfile
    image: media-server
    container_name: media-server
    ports:
      - "8081:8081"  
    volumes:
      - ./media:/media:ro
      - ./src/nginx.conf:/etc/nginx/conf.d/default.conf:ro
    networks:
      - servernet
    depends_on:
      - transcoding-service

networks:
  servernet: